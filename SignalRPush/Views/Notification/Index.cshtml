@model SignalRPush.Models.CreateNotificationModel

@{
    ViewData["Title"] = "View";
}

<h4>CreateNotificationModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateNotification" asp-controller="Notification" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-wrap">
                <input asp-for="Title" class="form-input" />
                <label asp-for="Title" class="control-label">Title</label>
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>
            <div class="form-wrap">
                <input asp-for="Content" class="form-input" />
                <label asp-for="Content" class="control-label">Content</label>
                <span asp-validation-for="Content" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Bildiris at" class="btn btn-success" />
            </div>
        </form>
    </div>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
